part of 'reveal_core.dart';

/// Direction of reveal transition.
enum RevealDirection {
  /// Opens the new content from the transition center.
  reveal,

  /// Closes the overlay into the transition center.
  conceal,
}
